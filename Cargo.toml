[workspace]
members = [ "kira-forwarding",
    "kira-lib",
    "kirad"
]
resolver = "2"

# Profile which optimizes the compiled binary for performance
[profile.benchmark]
inherits = "release"
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = "debuginfo"
debug = false
